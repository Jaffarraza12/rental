var lease  = {
        init:function () {
            $("#lease_prefer").change(function () {
                    /*if($(this).val() == 'Monthly'){
                            $(".date_range").fadeOut("fast",function () {
                                $(".single_date").fadeIn("fast")
                            });
                    } else {
                        $(".single_date").fadeOut("fast",function () {
                            $(".date_range").fadeIn("fast")
                        });
                    }*/
            })
        }
};